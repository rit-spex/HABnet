import React from 'react';

const SocketManager = React.createClass({
  render() {
    return(<h2>This component will handle socket initializtion and connecting to data sources </h2>);
  }
});

export default SocketManager;
